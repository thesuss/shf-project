- provide :signoff do
  = render 'application_mailer/signoff'

- message_scope = 'mailers.member_mailer.h_branding_fee_will_expire.message_text'

= render layout: 'layouts/mail_from_membership' do

  %p.fee-will-expire
    = t('expire_alert_html', scope: message_scope)
    %span.date-warning= "#{@expire_date}."

  = render 'how_to_pay_hmarkt', email: @recipient_email, company: @company
