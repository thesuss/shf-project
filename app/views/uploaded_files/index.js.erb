$('#uploaded-files-list').html("<%= j(render 'uploaded_files_list', locals: { search_params: @search_params }) %>");
// In case there is tooltip(s) in rendered element:
$('[data-toggle="tooltip"]').tooltip();
