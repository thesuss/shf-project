$('#uploaded-files-list').html("<%= j(render 'uploaded_files_list', search_params: @search_params, uploaded_files: @uploaded_files) %>");
// In case there is tooltip(s) in rendered element:
$('[data-toggle="tooltip"]').tooltip();
