-# this partial expects a local named
  app_config

%hr
.text-center
  = link_to "#{t('edit')} #{t('admin_only.app_configuration.show.title')}", admin_only_edit_app_configuration_path(app_config), class:'btn btn-info btn-sm'
  = link_to "#{t('back')}", :back, class: 'btn btn-light btn-sm'
