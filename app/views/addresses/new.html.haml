%header.entry-header
  %h1.entry-title= t('.title', company_name: @company.name)
.entry-content
  = render partial: 'form',
           locals: { address: @address, method: :post,
                     url: company_addresses_path(@company.id) }
  %hr
  .text-center
    = link_to "#{t('companies.view_company')}", @company, class: 'btn btn-sm btn-light'
