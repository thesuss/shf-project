%header.entry-header
  - unless current_user.admin?
    %h1.entry-title= t('.title')
  - if current_user.admin?
    %h1.entry-title= t('.admin_title')

.entry-content
  .row
    .col-lg-4
      = render 'map_companies',
               markers: location_and_markers_for(@all_visible_companies)


    .col-lg-8
      .col
        .search-instructions
          %p
            #{t('.h_companies_listed_below')}
            %br
            #{t('.how_to_search')}
      .col
        %button.btn.hide-search-form.btn-success.ml-auto{ id: 'toggle_search_form',
                                          href: '#company_search_form' }
          #{t('toggle.company_search_form.hide')}

      .panel.panel-default.search-form#company_search_form

        = render 'search_form', all_companies: @all_companies,
                                search_params: @search__params


  - if @companies.empty?
    %strong
      #{t('.no_search_results')}
  - else
    = render 'companies_list', companies: @companies, search_params: @search_params
